{
  "name": "quickbase-colorized-exports",
  "version": "1.0.0",
  "description": "Example of creating a function that runs as a cron job using the serverless `schedule` event",
  "author": "",
  "license": "MIT",
  "scripts": {
    "build": "yarn run uglifyjs quickbase-colors.js -o dist.js; echo \"javascript: $(cat dist.js)\" > dist.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.42.0",
    "chrome-aws-lambda": "^10.1.0",
    "dotenv": "^10.0.0",
    "puppeteer-core": "^11.0.0",
    "uglify-js": "^3.14.3"
  }
}
